version: '3'

services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    image: nodejs
    container_name: nodejs
    restart: unless-stopped
    env_file: .env
    environment:
      - PORT=$PORT
      - JWTTOKEN=$JWTTOKEN
      - HASH=$HASH
      - NODE_ENV=production
      - DB_USERNAME=$DB_USERNAME
      - DB_PASSWORD=$DB_PASSWORD
      - DB_HOST=$DB_HOST
      - DB_PORT=$DB_PORT
      - DB_NAME=$DB_NAME
      - FOLDER_ID=$FOLDER_ID
      - IMAGE_FOLDER_ID=$IMAGE_FOLDER_ID
      - AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY
      - AWS_CREDENTIALS_BUCKET=$AWS_CREDENTIALS_BUCKET
      - AWS_CREDENTIALS_FILE=$AWS_CREDENTIALS_FILE
      - AWS_TOKEN_FILE=$AWS_TOKEN_FILE
    ports:
      - "$PORT:8080"
    volumes:
      - .:/home/node/app
      - node_modules:/home/node/app/node_modules
    networks:
      - programming-courses

networks:
  programming-courses:
    driver: bridge

volumes:
  node_modules:  
